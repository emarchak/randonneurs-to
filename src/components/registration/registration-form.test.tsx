import React from "react"

describe('<RegistrationForm>', () => {
    it('renders all the required fields to the user', () => { })
    it('only shows bevets when registering for brevets', () => { })
    it('only shows permanents when registering for permanents', () => { })
    it('allows the rider to choose from all available routes', () => { })
    it('requires email and rider name', () => { })
    it('requires rider to be registered with the OCA', () => { })
    it('requires rider to acknowledge all policies', () => { })
    it('requires the start date to be > 1 week in the future', () => { })
    it('requires brevet start date to be within -2 / +1 weeks from scheduled date', () => { })
    it('allows the rider to choose a start time', () => { })
    it('limits registration to maximum of 10 riders per start time', () => { })
    it('records the registration when submitted', () => { })
    it('notifies riders when submitted', () => { })
    it('notifies rider organizerss when submitted', () => { })
})